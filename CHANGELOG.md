# 更新说明 / Changelog

## 2026-09-07 — 可靠性与安全修复

- ADB 调用改为参数化执行，检查设备数量、授权状态、超时和命令结果。
- 每条备忘录只读取一次 UI XML，并使用标准 XML 解析器同时提取标题、时间和正文。
- 使用列表滑动状态判断是否到底，避免空白/手绘记录导致死循环，也不再按正文前 100 字误删记录。
- 点击未进入详情页时安全停止；加入最大迭代次数保护。
- 保留 1～4 个字符的短备忘录，最后一屏空白位置不再生成虚假截图记录。
- 清理自定义输出名称，截图传输不再经过 shell，已有导出文件自动使用递增后缀而不会被覆盖。
- 无效模式会要求重新输入；脚本可安全导入，运行错误会输出明确原因。
- 修正 Windows 内附 ADB 的解压及运行说明，并使脚本自动查找已解压的 `platform-tools/adb.exe`。
- 统一 MIT License 说明，并修正文档中的占位仓库地址、耗时估计和贡献者署名。

> 本次没有可用的真实华为设备环境；改动采用保守的失败即停止策略，并通过静态检查和模拟测试验证。
